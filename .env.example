# Frontend (Vite) — defaults work out of the box via the dev proxy.
# Only set these if your backend is NOT on localhost:3001.
# VITE_API_URL=/api
# VITE_WS_URL=ws://localhost:5173/ws
VITE_APP_NAME=APSF Explorer
VITE_APP_VERSION=0.1.0
